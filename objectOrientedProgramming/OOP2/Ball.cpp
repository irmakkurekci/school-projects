#include "Ball.h"
void Ball::init(int size, string colour, string type, double price, double weight)
{
	 _size = size;
	 _colour = colour;
	 _type = type;
	 _price = price ;
	 _weight = weight ;
}